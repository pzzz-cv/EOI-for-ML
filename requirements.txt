# 视频稳定化系统依赖
# 安装命令：pip install -r requirements.txt
#
# PyTorch 需单独安装（根据 CUDA 版本选择）：
#   CUDA 12.1：pip install torch==2.3.0 torchvision==0.18.0 --index-url https://download.pytorch.org/whl/cu121
#   其他版本：https://pytorch.org/get-started/previous-versions/

# 核心深度学习框架（需单独安装，见上方注释）
# torch==2.3.0+cu121
# torchvision==0.18.0+cu121

# 图像处理
numpy==2.2.6
opencv-python>=4.11.0
Pillow==12.2.0
scikit-image==0.24.0

# 配置与工具
PyYAML==6.0.1
scipy>=1.15.3
thop>=0.1.0
matplotlib

# CUDA 加速（版本需与 CUDA 对应）
# CUDA 11.x：cupy-cuda11x
# CUDA 12.x：cupy-cuda12x
cupy-cuda12x>=14.0.0

# PNG 读写（RAFT 依赖）
pypng
